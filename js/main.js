// Inicializar AOS (aunque de momento no le hemos puesto data-aos a nada)
AOS.init({
  duration: 700,
  easing: "ease-out-quart",
  once: true
});

// Aquí iremos metiendo poco a poco interacciones con jQuery
$(document).ready(function () {
  console.log("STRICTO listo 🚀");
});
